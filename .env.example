# Meshtastic mesh channel PSK (256-bit, base64-encoded)
# All nodes in the mesh must share this key.
# Generate with: meshtastic --ch-set psk random --ch-index 0 --info
MESH_PSK="base64:REPLACE_WITH_YOUR_256BIT_PSK"
